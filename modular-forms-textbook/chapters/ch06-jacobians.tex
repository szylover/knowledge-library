% ============================================================================
\chapter{Jacobian 簇与模 Abel 簇}
\label{ch:jacobians}
% ============================================================================
% 对应 Diamond \& Shurman Chapter 6

前五章已经把三块关键材料分别准备好：
一方面，$X_0(N)$ 是紧黎曼面；
另一方面，$\Sk_2(\GammaO(N))$ 与其全纯微分空间同构；
再一方面，Hecke 算子在这些微分上给出彼此交换的算术作用。
本章的任务，就是把这三者压缩进一个真正的几何对象里：
Jacobian 簇 $J_0(N)$。

一旦 Jacobian 出现，我们就不再只是“研究模形式空间上的线性算子”，
而是能把 Hecke 代数看成 Abel 簇上的自同态环，进而从中切出与单个新形式对应的商 Abel 簇 $A_f$。
当 $f$ 的 Fourier 系数落在 $\Q$ 中时，这个因子就是一条椭圆曲线。
这正是模形式通向椭圆曲线、再通向模性定理的桥梁。

本章结构如下：
\begin{itemize}
  \item \textbf{§\ref{sec:jacobian-motivation}} 说明为什么必须引入 Jacobian。
  \item \textbf{§\ref{sec:abelian-varieties}} 回顾 Abel 簇、极化与同种。
  \item \textbf{§\ref{sec:jacobian-construction}} 构造 $J(X)$ 并解释 Abel--Jacobi 映射。
  \item \textbf{§\ref{sec:hecke-on-jacobian}} 说明 Hecke 代数如何作用在 $J_0(N)$ 上。
  \item \textbf{§\ref{sec:modular-abelian-varieties}} 定义模 Abel 簇 $A_f$ 并计算其维数。
  \item \textbf{§\ref{sec:jacobian-exercises}} 通过习题把这些概念真正落到具体级别 $N=11,37$ 上。
\end{itemize}

\input{chapters/ch06/s1-motivation}
\input{chapters/ch06/s2-abelian-varieties}
\input{chapters/ch06/s3-jacobian}
\input{chapters/ch06/s4-hecke-on-jacobian}
\input{chapters/ch06/s5-modular-abelian-variety}
\input{chapters/ch06/s6-exercises}
