# 第六卷《清至1912卷》进度

最后更新：2026-07-15（建立可编译的卷级骨架）

## 当前状态

| 项目 | 状态 | 说明 |
|---|---|---|
| 来源矩阵 | 完成 | `data/source-corpus/qing-1912.md` 已覆盖1644—1912的编年主干、档案、地方与多语材料、近代报刊、财政和物质证据，以及取证限制。 |
| 清初事件账本 | 完成 | `data/event-ledger/qing-1912.csv` 已登记 **80** 条节点，覆盖1644—1722；全部目标文件目前位于 `chapters/early-qing/`。 |
| 卷级编译基础 | 完成 | `main.tex`、暖色 `preamble.tex`、空章节与附录 wrappers、分期图目录和发布脚本已建立。 |
| 正文 prose | 进行中 | 已完成入关、北京接收、摄政与南方征服事件簇；后续 Narrative 任务只能使用现有账本 ID，并须先锁定对应正文文件。 |

## 清初账本覆盖

| 期间 | 已登记事件 | 对应正文目标 | prose 状态 |
|---|---:|---|---|
| 入关、征服与清初整合（1644—1722） | 80 | `chapters/early-qing/` | 已完成入关与摄政 1 个事件簇，其余待写 |

## 编译与发布约定

- 章节与附录 wrappers 仅提供章节标题和稳定标签；尚未接入事件正文或图件。
- 图件须按时期放入 `figures/<period>/`，并由未来章节 wrapper 引入。
- 只使用 `scripts/build-volume.ps1` 发布 `pdf/china-chronicle/vol06-qing-1912.pdf`；卷目录不保留构建产物。

## 下一步

1. 为1723—1912继续建立经核验的事件账本。
2. 在独立任务中认领并撰写已入账的清初事件簇。
3. 为完成的章节接入独立图件、来源说明与索引。

## 集成验收记录（2026-07-17）

- 状态：未验收通过。账本、构建和事件链接抽查通过，但 1723--1912 年账本、正文、
  图件和索引工作仍在待办；详情见 `../ACCEPTANCE_STATUS.md`。本轮未发布或同步 PDF。

## 进度核对（2026-07-17）

本节以当前工作树的实测结果为准，并替代上文 80 条清初初始事件池快照。主账本现为
**1,412** 条；正文有 14 个唯一事件锚点、14 个事件引用、14 个独立图件和 4 个
索引/附录文件。账本验证通过，未发现重复锚点或无目标引用。

**验收状态：未通过。** 1723--1912 的连续正文与深描、图件及索引质量、全卷
内容验收、构建复验和发布/Blob 可访问性仍未完成。

## 严格根验收（2026-07-17，`acceptance-qing-final`，最新 main）

**状态：未通过；严禁构建根目录 PDF、同步 Blob 或访问直链。** 按根规范的八项
验收复核，静态结果最多为 **37.62\%**：第 1、5、6 项通过；第 3 项即使将现有
14 个首次锚点都暂计作非模板叙事，也只有 `14 / 1,412 = 0.99\%`，故加权上限为
`(3 + 14/1412) / 8`。这低于 95\% 发布门槛。

| 根验收项 | 结果 | 本轮可复核证据 |
| --- | --- | --- |
| 1. 读法、纲要、共享年序、政权线、专题与章纲 | 通过（结构） | `how-to-read.tex`、六个 chapter wrapper、清初共享年序、各期专题及 `main.tex` 接入均存在。 |
| 2. 账本、来源矩阵与时间带×政权/地区缺口矩阵 | **不通过** | `validate-event-ledger.ps1` 通过 1,412 条 `acceptance-evidence-19` 记录，来源矩阵也存在；但现有 coverage CSV 只有时间带×行动者计数、逐年计数、字段表及三类来源限度汇总，缺逐格的政权/地区、目标/必要桥接、材料密度、来源限度、未覆盖事件类型与补充路径。 |
| 3. 中文锚点、最小证据链、因果链接与非模板正文 | **不通过** | 正文仅有 14 个唯一 `\eventanchor`，无重复、未知 ID 或悬空 `\eventref`，但 1,398 个账本 ID 没有首次呈现锚点；0.99\% 的覆盖不能达到 95\% 门槛。 |
| 4. 政权、地方、边疆、迁徙与慢变量的实质平衡 | **不通过** | 账本的行动者分配不能替代正文核验。14 个锚点不足以证明满洲、蒙古、新疆、青藏高原、东北、西南、台湾、海疆及各期地方行动者已有实质而非模板的平衡叙事。 |
| 5. 任务型信息框与材料边界 | 通过（静态） | `preamble.tex` 定义六种环境；正文各实际使用一次 `event`、`turningpoint`、`causalchain`、`textwindow`、`sourcenote` 与 `debate`。 |
| 6. 独立、可版本化图件 | 通过（静态） | `figures/` 有 14 份独立 TikZ 图件和 14 个实际图件标签，覆盖六个时期的时间轴及关系/空间图。 |
| 7. 正文锚点和读者索引 | **不通过** | 14 个事件导航引用均有目标，但 `appendix-sources.tex` 只有标题与标签，`appendix-index.tex` 缺政权及主题索引，并且图件索引仅列出 7/14 个实际图件标签。 |
| 8. 构建、链接、引用、重复锚点、进度、PDF 与发布同步 | **不通过（门槛阻断）** | 账本验证、14 个锚点的唯一性和 14 个事件引用的目标均已静态核验；因总分未达 95\%，未运行会写入根目录 PDF 的构建、`publish-downloads.ps1` 或 Blob 直链检查。 |

**精确阻断项：** Narrative 须为缺失的 **1,398** 个账本 ID 写入各自首次、可核且
非模板的叙事锚点；Source/Chronology 须补齐逐格时间带×政权/地区缺口矩阵；
Indexer 须补齐实际正文锚点的政权、主题、来源和全部图件索引。完成后再从最新
`main` 复跑八项验收；不得用批量锚点、桥接模板、账本计数或放宽检测代替这些工作。
